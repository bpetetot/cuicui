import React from 'react'

import Blockquote from '../../../../../src/blockquote'

const Basic = () => {
  return (
    <Blockquote text="Make everything as simple as possible, but not simpler." />
  )
}

export default Basic
